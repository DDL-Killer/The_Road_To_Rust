* Rust中char类型占4个字节
* 浮点数字面量的默认类型是f64
* 声明常量const必须显式指定类型
* type关键字可以给已有的类型定义别名
* vec长度可变,数据存放在堆上
* String是UTF-8编码的动态字符串
* &str是字符串切片,通常以借用形式出现
* Rust数组的长度在编译期就固定,无法动态修改
* s元组可以包含不同类型的元素,但长度固定不变
* 切片没有底层数据的所有权,不能直接修改源数据
* Rust不支持隐式类型转换,必须使用as做显式转换
* continue不会直接终止整个循环
* for循环可以直接遍历数组 范围等迭代器
* match匹配不允许遗漏任何可能的情况
* if let可以用来简化只有一个模式的匹配
* Rust允许给循环添加标签,用于多层循环跳转
* break后面可以跟返回值,让loop成为有返回值的表达式
* match中的 _ 通配符必须放在最后一个分支
* Rust函数不需要必须声明返回值类型
* 函数参数必须指定类型
* 函数最后一行表达式不加分号表示隐式返回
* return关键字可以让函数提前返回
* 关联函数不需要通过类型实例来调用
* String类型的数据存放在堆上
* 简单类型默认实现了Copy
* 结构体包含引用时，必须标注生命周期参数
* 同一作用域可以同时存在多个不可变引用
* 垂悬引用在Rust中会被编译器直接拒绝
* Rust源文件的固定拓展名是`.rs`
* Rust官方构建系统与包管理器是`cargo`
* 用于打印输出的println!是宏
* Vec数据存储在堆
* Rust中数组[T;N]的特点是编译器固定长度
* 关于切片(&[T])说法正确的是，只是引用，不拥有数据
* let声明的变量被同名新变量覆盖的特性叫做遮蔽
* Rust中if表达式的条件必须是布尔值
* Rust中for循环通常遍历迭代器
* Rust中match匹配必须覆盖所有可能情况
* 声明函数使用的关键字是fn
* 入口函数是main()
* 执行操作但不返回值的是语句
* 关联函数使用::符号调用
* Rust不支持多个所有者同时存在
* 属于堆上分配，会触发所有权移动的是String
* 实际发生的是所有权移动